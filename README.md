# text-to-video

UI lives in the `web` folder (Next.js app).

## Quick start

Run from repository root:

```bash
npm run install:web
npm run dev
```

Or run directly in `web`:

```bash
cd web
npm install
npm run dev
```
